# Series
  1.Series 객체는 파이썬 리스트와 달리 인덱싱 값을 지정할 수 있음
  2.index values 속성 접근가능
  3.인덱싱이 서로 다른 경우에도 알아서 인덱싱이 같은 값끼리 덧셈 연산을 수행
  

from pandas import Series, DataFrame

mine   = Series([10, 20, 30], index=['naver', 'sk', 'kt'])
friend = Series([10, 30, 20], index=['kt', 'naver', 'sk'])

merge = mine + friend
