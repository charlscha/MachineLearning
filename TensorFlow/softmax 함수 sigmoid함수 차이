# Softmax(소프트맥스)는 입력받은 값을 출력으로 0~1사이의 값으로 모두 정규화하며 출력 값들의 총합은 항상 1이 되는 특성을 가진 함수

# sigmoid 해당 뉴런으로 들어오는 입력들과 바이어스에 의해 출력 결정
# softmax 해당 뉴런으로 들어오는 입력들과 바이어스에 의해 출력이 결정되는 구조는 비슷
  다른 뉴런의 출력값과의 상대적인 비교를 통해 최종 출력값이 결정 (non-locality)
  
  즉 모든 뉴런의 출력값들의 합하면 1이 되기 때문 평균화의 효과를 얻는다.
